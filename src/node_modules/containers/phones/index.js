/* 
  Phones - React ,
  обернутый в декоратор connect для связи с Store

  Если строка составная, то ее обертывают в кавычки:
  <Link to={`/phones/${phone.id}`}>
*/

import React, {Component} from 'react'
import {connect} from 'react-redux'
// Для использования тегов <Link></Link>
import {Link} from 'react-router'
import R from "ramda";

// Импортируем модули
import {fetchPhones, loadMorePhones, addPhoneToBasket, fetchCategories} from 'actions'
import {getPhones} from 'selectors'

class Phones extends Component {
  componentDidMount () {
    this.props.fetchPhones() // Action приходит из props

    this.props.fetchCategories()

  }

  renderPhone (phone, index) {
    // вызываем деструктаризацией addPhoneToBasket из props
    const {addPhoneToBasket} = this.props

    // Метод выводит только первые 60 символов описания ( обернуто в строку ES-6)
    const shortDescription = `${R.take(60, phone.description)}...`
  
    return (
      <div className='col-sm-4 col-lg-4 col-md-4 book-list' key={index}>
        <div className='thumbnail'>
          <img
            className='img-thumbnail'
            src={phone.image}
            alt={phone.name}
          />
          <div className='caption'>
            <h4 className='pull-right'>${phone.price}</h4>
            <h4>
              <Link to={`/phones/${phone.id}`}>
                {phone.name}
              </Link>
            </h4>
            <p>{shortDescription}</p>
            <p className='itemButton'>
              <button
                className='btn btn-primary'
                onClick={() => addPhoneToBasket(phone.id)}
              >
                Buy Now!
              </button>
              <Link
                to={`/phones/${phone.id}`}
                className='btn btn-default'
              >
                More info
              </Link>
            </p>
          </div>
        </div>
      </div>
    )
  }
   
  render () {
    // Вызываем из props phones & loadMorePhones
    const {phones, loadMorePhones} = this.props
    return (
      <div>
      <div className='books row'>
        {phones.map((phone, index) => this.renderPhone(phone, index))}
      </div>

      <div className='row'>
        <div className='col-md-12'>
          <button
            onClick={loadMorePhones}
            className='pull-right btn btn-primary'
          >
            Load More
          </button>
        </div>
      </div>
    </div>
    )
  }
}

// Получаем phones из state в props.phones, и категорию из ownProps
const mapStateToProps = (state, ownProps) => {
  return {
    phones: getPhones(state, ownProps)
  }
}

const mapDispatchToProps = {
  // Сокращенная форма записи dispatch и т.д.
  fetchPhones,
  loadMorePhones, 
  addPhoneToBasket,
  fetchCategories
}
// Обетрываем export в декоратор connect
export default connect(mapStateToProps, mapDispatchToProps)(Phones)