/* 
Component React - BasketCart (sharable)
 
Данный компонент мы будем рендерить на странице товаров и отдельного товара.
*/

import React from 'react'
import {Link} from 'react-router'
import {connect} from 'react-redux'
import {getTotalBasketCount, getTotalBasketPrice} from 'selectors'

// Передаем аргумент деструктаризацией из props
const BasketCart = ({totalBasketCount, totalPrice}) => (
  <div className='cart'>
    <div className='dropdown'>
      <Link
        to='/basket'
        id='dLabel'
        className='btn btn-inverse btn-block btn-lg'
      >
        <i className='fa fa-fa-shopping-cart' />
        <span>{totalBasketCount} item(s) - ${totalPrice}</span>
      </Link>
    </div>
  </div>
)

// Кладем методы в props, чтобы потом вызвать их из selectors
const mapStateToProps = state => {
    return {
      totalBasketCount: getTotalBasketCount(state),
      totalPrice: getTotalBasketPrice(state)
    }
  }

  export default connect(mapStateToProps, null)(BasketCart)